namespace EventFeed.Producer.Clicks
{
    public class ClickedEvent
    {
        // interesting data typically goes here
        // note that the event doesn't contain the click count;
        // that is a transient property derived from the number of events handled
    }
}
